# 工程细胞后台字段与文件限制测试要求

更新时间：2026-06-15

## 1. 使用说明

本文面向测试人员编写测试用例，覆盖当前原型中可操作页面的字段限制、上传限制、导入限制和关键边界。

进度口径：

- P0：当前版本必须覆盖，影响登录、录入、新增、编辑、导入、删除等主流程。
- P1：建议本期补齐，主要是前端/后端一致校验、重复性、格式合法性和边界提示。
- P2：后续增强，主要是安全、审计、权限细化、批量导入错误报告。

约定：

- 文本长度按中文、英文、数字均计 1 个字符。
- 文本字段默认去除首尾空格后提交，纯空格视为空。
- 未特别说明的文本字段禁止输入 `<script>`、HTML 标签、SQL 片段等高风险内容，提交时应转义展示。
- 选择类字段只允许选择下拉选项，不允许提交非枚举值。
- 日期时间统一建议格式：`YYYY-MM-DD HH:mm:ss` 或页面控件格式，结束时间不得早于开始时间。
- 数值字段只允许阿拉伯数字，可按字段要求支持小数，禁止中文数字、科学计数法、单位混输。

## 2. 全局文件上传与导入规则

| 场景 | 支持格式 | 大小限制 | 必填内容 | P0 测试点 | P1 建议补齐 |
|---|---:|---:|---|---|---|
| 传感器批量导入 | `.xlsx`, `.csv` | 最大 10MB | 按模板列 | 正确格式成功、错误格式拦截、超限拦截 | 校验空文件、错列名、重复记录、部分成功/失败明细 |
| 工程细胞服务导入 | `.xlsx`, `.xls`, `.csv` | 最大 50MB，当前已实现 | 细胞编号、细胞名称、菌株类型 | 正确格式成功、`.txt` 拦截、50MB+1B 拦截 | 后端同步校验大小和 MIME，输出行级错误 |
| 基因型数据集上传 | `.vcf`, `.bed`, `.fam`, `.phe`, `.csv` | 单文件最大 500MB | 数据集名称、数据类型、菌株类型、数据大小 | 页面展示限制、保存元数据 | 当前无真实 file input，需补齐真实文件选择与校验 |
| 组学模型文件重新上传 | `.xml`, `.json`, `.mat` | 最大 200MB | 模型文件 | 编辑页展示限制 | 当前无真实 file input，需补齐真实文件选择与校验 |

文件通用边界：

- 文件名：1-120 字符，允许中文、英文、数字、下划线、中横线、点，禁止路径符号 `/ \ : * ? " < > |`。
- 空文件：禁止导入，提示“文件内容为空”。
- 重复文件：同一文件名和大小重复上传时应允许重新选择，但导入时按业务唯一键判断是否覆盖或新增。
- CSV 编码：优先支持 UTF-8，P1 支持 UTF-8 BOM，非 UTF-8 文件需提示编码错误。
- Excel：只解析第一个 Sheet，P1 建议提示“仅导入第一个工作表”。

## 3. 账号页面

### 3.1 登录页

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 用户名/邮箱 | 是 | 账号 4-20 位字母、数字、下划线，或合法邮箱，建议最大 64 字符 | 空、纯空格、3 位、4 位、20 位、21 位、非法邮箱 |
| 密码 | 是 | 8-20 位，需包含字母、数字、特殊字符 | 空、7 位、8 位弱密码、8 位强密码、20 位、21 位 |
| 验证码 | 是 | 4 位，大小写不敏感，必须与图形码一致 | 空、错误码、大小写混输、刷新后旧码失效 |
| 记住密码 | 否 | 布尔值 | 勾选/取消后登录流程正常 |

P0：空字段、错误验证码、正确登录跳转到物理传感器页。

### 3.2 注册页

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 用户名 | 是 | 4-20 位，字母、数字、下划线，当前已实现 | 3 位失败、4 位成功、20 位成功、21 位失败、中文失败 |
| 所属机构 | 是 | 2-50 字符，中文/英文/数字/括号/中横线 | 空、1 字、50 字、51 字、特殊符号 |
| 设置密码 | 是 | 8-20 位，含字母、数字、特殊字符，当前强度校验已实现最小 8 位 | 7 位失败、弱密码失败、强密码成功、21 位失败 |
| 确认密码 | 是 | 必须与设置密码一致 | 不一致失败、一致成功 |
| 图片验证码 | 是 | 4 位，与当前验证码一致 | 错误失败、刷新后旧码失败 |
| 协议勾选 | 是 | 必须勾选 | 未勾选失败、勾选成功 |

P1：补齐用户名重复校验、邮箱/手机号注册入口如后续增加需单独规则。

### 3.3 找回密码页

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 用户名 | 是 | 4-20 位字母、数字、下划线 | 空、非法字符、有效账号、不存在账号 |
| 图片验证码 | 是 | 4 位，与当前验证码一致 | 错误、刷新、大小写 |
| 新密码 | 是 | 8-20 位，含字母、数字、特殊字符 | 弱密码、强密码、与旧密码相同 |
| 确认密码 | 是 | 必须与新密码一致 | 不一致失败 |

P1：找回密码当前未校验密码强度与一致性完整规则，需与注册页保持一致。

## 4. 数据资源目录页

页面定位：只读目录、搜索筛选、查看详情。

| 字段/控件 | 类型/限制 | P0 测试点 |
|---|---|---|
| 关键字搜索 | 0-50 字符，匹配名称、分类、范围、字段、负责人、格式 | 空搜索、精确匹配、模糊匹配、无结果 |
| 数据分类 | 枚举：全部及页面分类 | 每个分类筛选结果正确 |
| 数据格式 | 枚举：全部、Parquet、CSV 等当前表格格式 | 格式筛选结果正确 |
| 状态 | 枚举：全部、已标准化等 | 状态筛选结果正确 |
| 查看详情 | 只读 | 打开详情、关闭详情、字段展示完整 |
| 分页 | 每页 10 条 | 首页、尾页、无结果分页 |

P1：搜索内容需防 XSS，筛选组合后分页页码应回到第 1 页。

## 5. 物理传感器页面

### 5.1 列表、筛选、操作

| 控件 | 限制 | P0 测试点 |
|---|---|---|
| 批次切换 | 只能选择已有批次 | 切换后记录列表、分页、状态统计正确 |
| 状态筛选 | 全部、正常、预警、异常 | 筛选结果正确，空结果展示正常 |
| 查看详情 | 只读 | 参数、批次、录入人、发酵周期展示完整 |
| 批量导入 | `.xlsx`, `.csv`，最大 10MB | 正确格式、错误格式、超限、取消 |
| 阈值配置 | 数值输入 | 保存、恢复默认、非法值拦截 |

### 5.2 新增物理参数录入

| 字段 | 必填 | 类型/限制 | 合理范围/阈值 | 边界用例 |
|---|---|---|---|---|
| 批次号 | 是 | 选择已有批次 | 已有批次 | 不选、切换批次 |
| 录入时间 | 是 | 日期时间，不能晚于当前时间 24 小时以上 | 当前时间附近 | 空、未来时间、非法格式 |
| 录入人员 | 是 | 2-20 字符，中文/英文/数字 | - | 1 字、20 字、21 字 |
| 开始发酵时间 | 是 | 日期时间 | 不晚于结束时间 | 空、晚于结束时间 |
| 结束发酵时间 | 是 | 日期时间 | 不早于开始时间 | 空、早于开始时间 |
| 温度(℃) | 是 | 数值，1 位小数 | 25.0-38.0，预警 37.0 | 24.9、25.0、37.0、38.0、38.1 |
| 罐内压力(kPa) | 是 | 数值，1 位小数 | 0.8-1.5，预警 1.4 | 0.7、0.8、1.4、1.5、1.6 |
| 搅拌速度(rpm) | 是 | 整数 | 100-500，预警 450 | 99、100、450、500、501 |
| 气体流量(L/min) | 是 | 数值，1 位小数 | 0.5-3.0，预警 2.8 | 0.4、0.5、2.8、3.0、3.1 |
| pH值 | 是 | 数值，2 位小数 | 6.0-8.0，预警 7.5 | 5.99、6.00、7.50、8.00、8.01 |
| 溶解氧浓度(mg/L) | 是 | 数值，1 位小数 | 2.0-8.0，预警 7.0 | 1.9、2.0、7.0、8.0、8.1 |
| 溶解CO2浓度(mg/L) | 是 | 数值，1 位小数 | 10.0-100.0，预警 90.0 | 9.9、10.0、90.0、100.0、100.1 |
| 排气O2分压(%) | 是 | 数值，1 位小数 | 15.0-21.0，预警 20.0 | 14.9、15.0、20.0、21.0、21.1 |
| 排气CO2分压(%) | 是 | 数值，2 位小数 | 0.03-0.10，预警 0.09 | 0.02、0.03、0.09、0.10、0.11 |

提交规则：

- 暂存允许未完成字段，但至少需有批次号和录入人员。
- 提交必须校验所有必填字段。
- 任一参数超过最大/最小范围，状态为异常；达到预警阈值但未越界，状态为预警；全部正常为正常。

## 6. 生物传感器页面

### 6.1 新增代谢物录入

| 字段 | 必填 | 类型/限制 | 合理范围 | 边界用例 |
|---|---|---|---|---|
| 批次号 | 是 | 选择已有批次 | 已有批次 | 不选、切换批次 |
| 发酵物名称 | 是 | 2-50 字符 | - | 空、1 字、50 字、51 字 |
| 录入时间 | 是 | 日期时间 | 不晚于当前时间 24 小时以上 | 空、未来时间 |
| 录入人员 | 是 | 2-20 字符 | - | 空、1 字、21 字 |
| 开始发酵时间 | 是 | 日期时间 | 不晚于结束时间 | 结束早于开始 |
| 结束发酵时间 | 是 | 日期时间 | 不早于开始时间 | 空、非法格式 |
| 谷氨酸(g/L) | 是 | 数值，2 位小数 | 0-50 | -0.01、0、50、50.01 |
| 葡萄糖(g/L) | 是 | 数值，2 位小数 | 0-100 | -0.01、100.01 |
| 丙酮酸(g/L) | 是 | 数值，2 位小数 | 0-10 | -0.01、10.01 |
| 甘氨酸(g/L) | 是 | 数值，2 位小数 | 0-5 | -0.01、5.01 |
| 谷氨酰胺(g/L) | 是 | 数值，2 位小数 | 0-8 | -0.01、8.01 |
| 精氨酸(g/L) | 是 | 数值，2 位小数 | 0-5 | -0.01、5.01 |
| 组氨酸(g/L) | 是 | 数值，2 位小数 | 0-3 | -0.01、3.01 |
| 色氨酸(g/L) | 是 | 数值，2 位小数 | 0-4 | -0.01、4.01 |
| 乳酸(g/L) | 是 | 数值，2 位小数 | 0-15 | -0.01、15.01 |
| 铵根离子(mmol/L) | 是 | 数值，2 位小数 | 0-200 | -0.01、200.01 |
| 柠檬酸(g/L) | 是 | 数值，2 位小数 | 0-10 | -0.01、10.01 |
| 亮氨酸(g/L) | 是 | 数值，2 位小数 | 0-6 | -0.01、6.01 |
| 异亮氨酸(g/L) | 是 | 数值，2 位小数 | 0-4 | -0.01、4.01 |
| 甲硫氨酸(g/L) | 是 | 数值，2 位小数 | 0-3 | -0.01、3.01 |
| 半胱氨酸(g/L) | 是 | 数值，2 位小数 | 0-2 | -0.01、2.01 |
| 赖氨酸(g/L) | 是 | 数值，2 位小数 | 0-8 | -0.01、8.01 |

阈值配置 P0：

- 最小值必须小于最大值。
- 预警值必须大于等于最小值且小于等于最大值。
- 生物传感器阈值当前只配置前 10 项，P1 建议补齐 16 项。

导入 P0：

- 支持 Excel、CSV，最大 10MB。
- 导入前需下载模板。
- 生物传感器当前是确认式导入，需测试取消、确认、重复点击。

## 7. 基因型表型关联分析页

### 7.1 新增/编辑分析项目

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 项目名称 | 是 | 2-50 字符，建议同租户内唯一 | 空、1 字、50 字、重复名称 |
| 菌株类型 | 是 | 大肠杆菌、酵母菌、芽孢杆菌 | 枚举逐项 |
| 表型类型 | 是 | 产量、生长速率、底物利用率 | 枚举逐项 |
| 分析方法 | 是 | GWAS分析、关联分析、候选位点分析 | 枚举逐项 |
| 显著性阈值 | 是 | 数值，0-1，建议默认 0.05，最多 6 位小数 | -0.01、0、0.05、1、1.01 |
| 项目描述 | 否 | 0-500 字符 | 空、500 字、501 字、特殊字符 |

### 7.2 上传数据集

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 数据集名称 | 是 | 2-50 字符 | 空、重复名称、超长 |
| 数据类型 | 是 | VCF、BED、FAM、PHE、CSV | 枚举逐项 |
| 菌株类型 | 是 | 大肠杆菌、酵母菌、芽孢杆菌 | 枚举逐项 |
| 数据大小 | 是 | 正数，单位 MB/GB，展示字段需与真实文件大小一致 | 0、负数、非法单位 |
| 上传文件 | 是 | `.vcf`, `.bed`, `.fam`, `.phe`, `.csv`，最大 500MB | 正确格式、错误格式、500MB、500MB+1B |

P1：当前上传弹窗只保存元数据，需增加真实文件选择、文件大小自动读取、数据类型与后缀一致校验。

## 8. 组学数据分析页

### 8.1 新建/编辑代谢模型

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 模型名称 | 是 | 2-50 字符，建议唯一 | 空、1 字、50 字、重复名称 |
| 菌株类型 | 是 | 大肠杆菌、酵母菌、芽孢杆菌 | 枚举逐项 |
| 模型来源 | 是 | 文件导入、数据库导入、手动构建 | 枚举逐项 |
| 模型描述 | 否 | 0-500 字符 | 空、500 字、501 字 |
| 模型文件 | 编辑时可重新上传 | `.xml`, `.json`, `.mat`，最大 200MB | 正确格式、错误格式、超限 |

P0：新增、编辑、删除、详情查看。

P1：新建时如模型来源为“文件导入”，应要求上传模型文件。

## 9. 发酵过程分析页

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 模型名称 | 是 | 2-50 字符，建议唯一 | 空、超长、重复 |
| 模型类型 | 是 | 代谢网络模型、神经网络模型 | 枚举逐项 |
| 菌株类型 | 是 | 谷氨酸棒杆菌、大肠杆菌、酵母菌、芽孢杆菌 | 枚举逐项 |
| 优化目标 | 是 | 产量最大化、生成速率最大化、底物利用最大化 | 枚举逐项 |
| 模型描述 | 否 | 0-500 字符 | 空、超长、特殊字符 |

P0：新建、编辑、详情、删除。

P1：模型名称重复校验、描述敏感字符转义。

## 10. 全流程数据分析页

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 项目名称 | 是 | 2-50 字符，建议唯一 | 空、1 字、50 字、重复 |
| 菌株类型 | 是 | 谷氨酸棒杆菌、大肠杆菌、酵母菌、芽孢杆菌 | 枚举逐项 |
| 项目分析描述 | 否 | 0-500 字符 | 空、超长 |
| 选择分析模块 | 是 | 至少选择 1 个，可多选 genotype、omics、process、patent 等当前选项 | 0 个失败、1 个成功、全选成功 |

P0：新建、编辑、模块勾选保存、关联详情打开、删除。

P1：至少选择 1 个分析模块当前需补齐硬校验。

## 11. 工程细胞服务页

### 11.1 导入新增

| 限制项 | 要求 | 当前状态 |
|---|---|---|
| 文件格式 | `.xlsx`, `.xls`, `.csv` | 已实现 |
| 文件大小 | 不超过 50MB | 已实现 |
| 必填列 | 细胞编号、细胞名称、菌株类型 | 页面说明有，P1 需硬校验 |
| 可选列 | 基因型、状态、描述、培养编号、培养方式、培养基、温度、pH、转速、培养状态、优化记录、检测编号、检测项目、检测方法、细胞活性、细胞纯度、检测结果、检测说明、应用编号、场景类型、应用领域、应用状态、应用内容 | 后端导入支持映射 |
| 默认值 | 缺少可选列时使用默认值 | 当前已部分实现 |

导入模板列名兼容：

- 细胞名称：工程细胞名称、细胞名称、名称、name。
- 细胞编号：工程细胞编号、细胞编号、编号、code。
- 菌株类型：菌株、菌株类型、strain。
- 基因型：基因型、基因、gene。
- 状态：状态、验证状态、status。

P0 测试：

- 正确 CSV 导入成功。
- 正确 XLSX 导入成功。
- `.txt` 文件被拦截。
- 50MB 文件成功，50MB+1B 文件失败。
- 未选择文件时“开始导入”不可点击。

### 11.2 手动新增/编辑，步骤 1 基础信息

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 细胞编号 | 是 | 2-30 字符，英文/数字/中横线/下划线，建议唯一 | 空、重复、超长 |
| 细胞名称 | 是 | 2-50 字符 | 空、1 字、50 字、51 字 |
| 菌株类型 | 是 | 谷氨酸棒杆菌、大肠杆菌、酵母菌、芽孢杆菌 | 枚举逐项 |
| 状态 | 是 | 已验证、验证中、待验证 | 枚举逐项 |
| 基因型 | 否 | 0-500 字符 | 空、超长、特殊字符 |
| 细胞描述 | 否 | 0-500 字符 | 空、超长 |

### 11.3 步骤 2 培养信息

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 培养编号 | 是 | 2-30 字符，建议唯一 | 空、重复、超长 |
| 培养方式 | 是 | 摇瓶培养、发酵罐培养、微孔板培养 | 枚举逐项 |
| 培养温度(℃) | 是 | 数值，0-100，1 位小数 | -0.1、0、37.0、100、100.1 |
| pH值 | 是 | 数值，0-14，2 位小数 | -0.01、0、7.00、14、14.01 |
| 转速(rpm) | 是 | 整数，0-2000 | -1、0、200、2000、2001 |
| 培养状态 | 是 | 已完成、优化中、待验证 | 枚举逐项 |
| 培养基配方 | 否 | 0-1000 字符 | 空、超长 |
| 优化记录 | 否 | 0-1000 字符 | 空、超长 |

### 11.4 步骤 3 检测分析

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 检测编号 | 是 | 2-30 字符，建议唯一 | 空、重复、超长 |
| 检测项目 | 是 | 细胞活性检测、细胞纯度检测、表达稳定性检测、代谢产物检测 | 枚举逐项 |
| 检测方法 | 是 | 流式细胞术、HPLC、显微成像、酶活分析 | 枚举逐项 |
| 细胞活性 | 是 | 高、中、低 | 枚举逐项 |
| 细胞纯度 | 是 | 高、中、低 | 枚举逐项 |
| 检测结果 | 是 | 符合预期、需优化、待复核 | 枚举逐项 |
| 检测描述 | 否 | 0-500 字符 | 空、超长 |

### 11.5 步骤 4 应用信息

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 应用编号 | 是 | 2-30 字符，建议唯一 | 空、重复、超长 |
| 应用场景类型 | 是 | 生物制造、合成生物学、医药研发、农业应用 | 枚举逐项 |
| 应用领域 | 是 | 2-50 字符 | 空、1 字、50 字、51 字 |
| 应用状态 | 是 | 已验证、验证中、待验证 | 枚举逐项 |
| 工程化应用信息内容 | 否 | 0-1000 字符 | 空、超长、特殊字符 |

P0：手动新增需测试上一步、下一步、最后一步提交、编辑回显、删除。

P1：当前步骤切换未强制校验必填，建议下一步前校验当前步骤必填字段。

## 12. 系统管理

### 12.1 用户管理

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 用户名 | 是 | 4-20 位字母、数字、下划线，唯一 | 3 位、4 位、20 位、重复、中文 |
| 姓名 | 是 | 2-20 字符，中文/英文 | 空、1 字、20 字、21 字 |
| 手机号 | 是 | 中国大陆手机号，11 位，`1` 开头 | 10 位、11 位、12 位、非法号段 |
| 性别 | 是 | 男、女、未知 | 枚举逐项 |
| 邮箱 | 是 | 合法邮箱，最大 64 字符 | 无 @、中文邮箱、超长 |
| 所属机构 | 是 | 2-50 字符 | 空、超长 |
| 状态 | 是 | 启用、停用 | 状态切换 |

列表筛选：用户名、姓名、手机号、状态。P0 测试筛选、重置、新增、编辑、启停、删除。

### 12.2 角色管理

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 角色名称 | 是 | 2-30 字符，唯一 | 空、重复、超长 |
| 角色编码 | 是 | 2-30 位小写字母、数字、下划线，唯一 | 大写、中文、重复 |
| 角色类型 | 是 | 系统内置、自定义 | 枚举逐项 |
| 状态 | 是 | 启用、停用 | 启停 |

P1：系统内置角色建议禁止删除或提示二次确认。

### 12.3 菜单管理

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 菜单名称 | 是 | 2-30 字符，同层级唯一 | 空、重复 |
| 菜单类型 | 是 | 目录、菜单、按钮 | 枚举逐项 |
| 路由地址 | 条件必填 | 目录/菜单以 `/` 开头，按钮可为 `-` | 空、无 `/`、按钮 `-` |
| 权限标识 | 是 | 2-80 字符，格式建议 `module:resource:action` | 空、中文、重复 |
| 显示状态 | 是 | 显示、隐藏 | 枚举逐项 |
| 排序 | 是 | 0-999 的整数 | -1、0、999、1000、小数 |

### 12.4 操作日志

| 字段/控件 | 类型/限制 | P0 测试点 |
|---|---|---|
| 操作人筛选 | 0-20 字符 | 精确、模糊、无结果 |
| 操作模块 | 全部、用户管理、角色管理、菜单管理、平台配置 | 枚举逐项 |
| 操作结果 | 全部、成功、失败 | 枚举逐项 |
| IP地址 | IPv4 格式 | 合法、非法、空 |
| 导出日志 | 建议 `.xlsx` 或 `.csv` | 导出确认、取消、无数据导出 |

P1：导出需支持时间范围、最大导出条数和权限控制。

### 12.5 平台配置

| 字段 | 必填 | 类型/限制 | 边界用例 |
|---|---|---|---|
| 配置名称 | 是 | 2-50 字符，唯一 | 空、重复、超长 |
| 配置编码 | 是 | 2-80 字符，格式建议 `group.name.key`，唯一 | 空、中文、重复 |
| 配置分组 | 是 | 基础配置、安全配置、通知配置、存储配置 | 枚举逐项 |
| 当前值 | 是 | 1-500 字符，按配置类型进一步限制 | 空、超长、特殊字符 |
| 状态 | 是 | 启用、停用 | 启停 |

关键配置建议专项规则：

- 登录失败锁定次数：整数 1-10。
- 会话超时时间：整数 5-1440 分钟。
- 对象存储桶名称：3-63 字符，小写字母、数字、中横线。

## 13. 通用测试用例设计清单

每个新增/编辑页至少覆盖：

- 必填字段全部为空。
- 单个必填字段为空。
- 最小长度 -1、最小长度、最大长度、最大长度 +1。
- 数值最小值 -1 个精度、最小值、预警值、最大值、最大值 +1 个精度。
- 下拉字段逐项选择。
- 文本字段输入首尾空格、纯空格、特殊字符、HTML 标签。
- 保存后列表展示、详情展示、编辑回显一致。
- 删除二次确认、取消删除、确认删除后列表刷新。
- 重复点击提交按钮，不应产生重复记录。
- 刷新页面后 localStorage 持久化数据仍存在。

每个导入/上传页至少覆盖：

- 未选择文件时按钮状态。
- 正确后缀、错误后缀、大小等于上限、大小超过上限。
- 空文件、只有表头、缺少必填列、必填列为空、重复业务主键。
- 中文文件名、超长文件名、带特殊字符文件名。
- 导入成功后列表记录数、详情字段、状态标签正确。
- 导入失败时错误提示明确，不写入脏数据。

## 14. 当前实现差距，建议排期

P0 建议立刻确认：

- 账号密码规则在注册、找回、登录三处保持一致。
- 物理/生物传感器提交时补齐必填、数值范围和时间先后校验。
- 工程细胞导入已有限制，需保证后端同样校验格式和 50MB 大小。
- 全流程分析至少选择 1 个模块。

P1 建议下个小版本：

- 所有新增/编辑弹窗补齐字段级错误提示，不只用全局 toast。
- 唯一性校验：用户名、手机号、邮箱、项目名称、模型名称、细胞编号、角色编码、配置编码。
- 导入结果提供成功数、失败数、失败行号、失败原因。
- 基因型数据集和组学模型文件补齐真实 file input 与文件读取校验。

P2 后续增强：

- 权限控制：系统内置角色/菜单/配置删除限制。
- 审计：新增、编辑、删除、导入、导出都写操作日志。
- 安全：上传文件 MIME 校验、病毒扫描、服务端 XSS 转义、限流。
