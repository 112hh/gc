# 工程细胞数据平台 — UI 规范核查与优化报告

生成时间：2026-08-06
依据：`design-system/MASTER.md`（Ant Design 蓝、8px 间距节奏、Header+Sider+Content、避免深色侧边栏、选中态 `#E6F4FF` 底 + `#1677FF` 字）

## 一、检查范围
| 页面 | 类型 | 是否引用共享 styles.css | 规范符合度 |
|------|------|------------------------|-----------|
| index.html | 主题库门户 | 否（自带样式） | 门户营销风，字体需统一 |
| standards.html | 标准体系后台 | 否（自带样式） | 基本符合（浅色侧栏、Ant 蓝） |
| app.html | 后台入口 | 是 | 符合（走共享样式） |
| engineering-cell-ingestion.html | 工程细胞入库后台 | 否（自带+Tailwind CDN） | **问题最多，已修复** |
| optimized_engineering_cell_ingestion.html | 入库后台·优化版 | 否（自带样式） | 符合（已是重做后的正确版） |
| 工程细胞数据应用平台_30页单文件原型.html | 30 页单文件原型 | 否（自带样式） | 符合（浅色侧栏、Ant 蓝） |

## 二、发现的主要问题
1. **配色不统一（最严重）**：`engineering-cell-ingestion.html` 使用 Arco 设计色——主色 `#165DFF`、成功 `#00B42A`、警告 `#FF7D00`、危险 `#F53F3F`、文字 `#1D2129`。而其余后台页统一用设计系统规定的 Ant 蓝（`#1677FF` 等）。导致全站出现"两种蓝 / 两种绿 / 两种红 / 两种文字灰"。
2. **深色侧边栏违规**：该页侧栏为实色深蓝 `#165DFF` + 白字，违反设计系统「避免深色侧边栏」与「选中态用 `#E6F4FF` 底 + `#1677FF` 字」的规定；其余后台页（standards / optimized / 30页）均为浅色侧栏。
3. **字体不统一**：各页 `font-family` 的组成与顺序不一致（有的以 `Segoe UI` 开头、有的缺少 `Hiragino Sans GB` / `Helvetica Neue`）。
4. **结构性不一致（建议项）**：头部高度 56/60/66px、侧栏宽度 220/240/248/260px 各页不一；30页原型强制 `width=1200` / `min-width:1200px` 不可响应；optimized 页残留一段被覆盖的旧深色侧栏 CSS（无害但冗余）。

## 三、已完成的优化（可直接查看）
### engineering-cell-ingestion.html（核心修复）
- **配色全量对齐**：整页 Arco 色值批量替换为设计系统 Ant 蓝（`#1677FF / #4096FF / #52C41A / #FAAD14 / #FF4D4F`，文字 `#262626 / #595959 / #8C8C8C`，边框 `#D9D9D9`，浅蓝 `#E6F4FF`），Tailwind config 调色板同步更新。
- **深蓝侧栏 → 浅色**：改为与 optimized / 30页 完全一致的浅蓝渐变（`#DCEEFF→#CFE5FF→#EDF6FF`）+ 深色文字；选中态 = `#E6F4FF` 底 + `#1677FF` 字 + 左侧 3px 蓝色高亮条；侧栏内全部白色/半透明白文字已翻转（仅保留蓝色头像圆上的白字"A"）。
- **字体统一**：改为规范字体栈。

### 其余页面
- `index.html` / `optimized_engineering_cell_ingestion.html`：字体统一为规范栈。
- `standards.html`、`30页原型`、`app.html`：已符合规范，未改动。

## 四、建议项（未自动改动，需你确认）
1. 统一后台头部高度（建议 56px）与侧栏宽度（建议 220px）。
2. 30页原型取消强制 `width=1200` / `min-width:1200px`，改为响应式（窄屏时侧栏收起或转为顶部导航）。
3. 清理 optimized 页中被覆盖的旧深色侧栏冗余 CSS（约前 60 行），避免误导后续维护。
4. 门户页 `index.html` 的 `#0d1b2a` 深色页脚属门户营销风格，与后台规范不同属正常，可保留。

## 五、验证结果
- 工程细胞页已无 `#165DFF / #00B42A / #FF7D00 / #F53F3F / #1D2129 / #4E5969 / #86909C / #E5E6EB / #E8F3FF` 等 Arco token 残留。
- 侧栏背景已为浅蓝渐变；选中导航态为 `#E6F4FF` 底 + `#1677FF` 字。
- 侧栏区域已无残留的不可见白字（彩色头像除外）。
